{
  "name": "blackdog-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for BlackDog NFT rental marketplace",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy-sepolia.js --network sepolia",
    "deploy:local": "hardhat run scripts/deploy-sepolia.js --network localhost",
    "verify:sepolia": "hardhat verify --network sepolia"
  },
  "keywords": ["nft", "rental", "marketplace", "ethereum", "sepolia"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
